export const Header = () => {
  return `

  <header class="h-16 border-b border-gray-800 flex items-center px-6">

    <input
      placeholder="Search..."
      class="bg-gray-800 px-4 py-2 rounded w-64 outline-none"
    />

  </header>

  `;
};
